# Local Perf

Quick and dirty directory for doing local perf testing (CoreDNS and client on same machine).

Quick and dirty:

* Checking in binaries
* Hardcoding stuff

See Makefile for running tests and docs (a little).
